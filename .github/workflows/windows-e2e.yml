name: Trace Windows E2E

on:
  workflow_dispatch:
  push:
    branches:
      - codex/windows-e2e
      - codex/windows-capture-release

jobs:
  build-and-test:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install frontend dependencies
        run: npm ci --ignore-scripts

      - name: Test tracker reducer and review engine
        run: npm run tracker:test

      - name: Test live capture transport and immediate match registration
        run: |
          cargo test --manifest-path src-tauri/Cargo.toml preserves_permessage_deflate_context_across_server_messages
          cargo test --manifest-path src-tauri/Cargo.toml windows_upstream_supports_more_than_reserved_port_pool
          cargo test --manifest-path src-tauri/Cargo.toml storage::tests::deduplicates_raw_operations_and_round_trips_reviews
          cargo test --manifest-path src-tauri/Cargo.toml cards::tests

      - name: Build Windows NSIS package
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          TRACE_SYNC_API_URL: ${{ vars.TRACE_SYNC_API_URL }}
        run: npm run tauri:build -- --bundles nsis

      - name: Upload Windows package and executable
        uses: actions/upload-artifact@v4
        with:
          name: trace-windows-x64
          if-no-files-found: error
          path: |
            src-tauri/target/release/match-lens.exe
            src-tauri/target/release/bundle/nsis/*.exe
